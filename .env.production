# 生产环境配置
VITE_APP_TITLE=FE Deploy
VITE_API_BASE_URL=https://api.example.com
VITE_APP_ENV=production
# Sentry(DSN 是公开的,可放 env;版本号 CI 注入为 git SHA)
VITE_APP_VERSION=production
VITE_SENTRY_DSN=
# 依赖外置:把 react/react-dom 通过 esm.sh CDN 加载,不打包进 bundle
EXTERNALS_CDN=true
